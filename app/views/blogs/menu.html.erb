<% content_for :redirect_js do %>
  window.location = '<%= "#{root_path}#/blogs/#{@blog.to_param}/menu" %>';
<% end %>
<%= render partial: "shared/menu", locals: { blog: @blog, is_menu_bar: false } %>